const Landmark = (landmark) => {
    return `
    <div class="sweden--landmark--name">${landmark.name}</div>
    <div class="sweden--landmark--description">${landmark.description}</div>
`
}

export default Landmark