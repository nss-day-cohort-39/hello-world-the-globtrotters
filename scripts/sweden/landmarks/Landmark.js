const Landmark = (landmark) => {
    return `
    <div class="swedish__landmark__names">${landmark.name}</div>
    <div class="swedish__landmark__description">${landmark.description}</div>
`
}

export default Landmark